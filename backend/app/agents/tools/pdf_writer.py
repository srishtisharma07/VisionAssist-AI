class PDFWriterTool:

    def save(self, text: str):
        raise NotImplementedError(
            "PDF Writer Tool will be implemented in the next milestone."
        )


pdf_writer = PDFWriterTool()